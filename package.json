{
  "name": "sms-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "full": "concurrently \"npm:backend\" \"npm:frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
